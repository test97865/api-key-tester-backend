name = "api-key-tester-api"
main = "src/index.js"
compatibility_date = "2025-11-10"

# D1 数据库绑定
[[d1_databases]]
binding = "DB"
database_name = "api-key-tester-db"
database_id = "" # 运行 wrangler d1 create api-key-tester-db 后填入

# 环境变量
[vars]
ENVIRONMENT = "production"

# Workers AI 绑定（可选，用于高级功能）
# [ai]
# binding = "AI"